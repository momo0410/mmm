# 2026-06-22 工作日志

## 联网检索能力落地（handoff-03 第十一节 7 步实施）

完成 docs/handoff-03-online-search-design.md 第十一节定义的全部 7 个 Step：

### Step 1: OnlineSearchService 骨架
- 新建 `src-python/app/services/online_search/` 目录
- `__init__.py` / `cache.py`（三级缓存 L1/L2/L3）/ `nvd_client.py`（NVD API v2.0，stdlib urllib，限流5次/30秒+指数退避重试）/ `registry.py`（统一入口，预算控制默认10次/渗透，去重，离线兜底）

### Step 2: 注册 search_cve 工具
- 修改 `tool_registry.py`：新增 `ONLINE_SEARCH_TOOL_SPECS`，kind="online_search"，build_tool_registry() 始终注入
- 修改 `executor.py`：online_search 工具走被动探测，run() 入口加守卫防误执行
- 修改 `agent.py`：初始化 OnlineSearchService 单例，`_execute_planned_tasks` 拦截 online_search 工具路由到 service.call()
- 新增 `_run_online_search_task` / `_extract_online_search_args`（JSON+正则容错）/ `_format_online_search_result`

### Step 3: 写 search-cve builtin skill
- `skills/builtin/search-cve/SKILL.md`，完整 v2.0 五段式

### Step 4: 端到端验证
- 7 项集成测试全部通过：工具注册、skill 解析、SkillMatcher 注入、executor.list_tools、executor 守卫、args 容错、结果格式化

### Step 5: 扩展剩余 3 个工具
- `msf_module_client.py`：内置 6 个高频模块离线KB + Rapid7 抓取兜底
- `default_creds_client.py`：内置 15+ 产品默认凭据数据集 + cirt.net 兜底
- 3 个 builtin skill：search-exploit / search-msf-module / search-default-creds
- 全部 4 个 skill 加载验证通过

### Step 6: 自升级闭环
- 修改 `skill_generator.py`：`__init__` 接收 `online_search_results` 参数
- 新增 `_build_online_search_context`：从检索日志提取相关 CVE/MSF/凭据信息
- 修改 `_build_v2_generation_prompt`：注入权威信息，要求 Principle 以 NVD 为准
- 修改 `agent.py`：渗透结束生成 skill 时传入 `online_search_service.get_results_log()`

### Step 7: NVD 反查修正脚本
- `scripts/verify_skills_with_nvd.py`：遍历 17 个 exploit skill，查 NVD，对比 Principle，差异大时 --fix 由 LLM 修正
- 单 skill 测试通过（CVE-2004-2687，NVD 返回 CVSS 9.3）

### 配置
- `settings.py` 新增 `OnlineSearchSettings`（enabled/max_calls/api_keys/timeout/cache_dir）

### 测试结果
- 所有新增/修改的 .py 文件 py_compile 通过
- 集成测试：工具注册 ✓ / skill 解析 ✓ / executor 守卫 ✓ / args 容错 ✓ / 结果格式化 ✓ / 4 客户端分发 ✓ / prompt 注入 ✓
